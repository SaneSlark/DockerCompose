services:
  homer:
    image: b4bz/homer:latest
    container_name: homer
    network_mode: bridge
    environment:
      - INIT_ASSETS=1
      - UID=1000
      - GID=1000
    ports:
      - "2281:8080"
    volumes:
      - ./:/www/assets
    restart: unless-stopped
